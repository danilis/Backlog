using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace WindowsFormsApplication10
{
    class DataManager
    {

        // get all sprint days
        // return array with sprint days
        public int[] get_all_sprint_days()
        {
            int[] ans = { };

            return ans;
        }

        // get expected work hours in sprint
        public int get_all_expected_hours()
        {
            int ans = 0;

            return ans;
        }
        // retun number of done hours
        public int get_all_work_hours()
        {

            return 0;
        }

        // return hors to finish sprint
        public int get_remain_hours()
        {
            int ans = 0;

            return ans;
        }

        // get number of days to sprint end
        public int get_remain_days()
        {
            int ans = 0;

            return ans;
        }

        //
        public int get_sprint_passed_days()
        {
            int ans = 0;

            return ans;
        }



        // connect to database
        private void db_connect()
        {
                        
        }

        // connect to database
        private void db_disconnect()
        {

        }
    }
}
